# Lab Checklist — GitHub Codespaces (Editor + Terminal Git)

- [x] Open the repo in Codespaces  
- [x] Run `python hello_world.py` and confirm output  
- [x] Edit `hello_world.py` in the Codespaces editor to include your name  
- [x] Run `git status` in the terminal  
- [x] Run `git add hello_world.py`  
- [x] Run `git commit -m "Update greeting"`  
- [x] Run `git push`  
- [x] Edit `hello_world.py` on the GitHub website and commit to main  
- [x] Run `git pull origin main` in the terminal  
- [x] Run `python hello_world.py` to confirm the web change  
- [x] Submit output from `bash verify.sh` to instructor
